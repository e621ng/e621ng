SELECT
  posts.id,
  posts.uploader_id,
  posts.created_at,
  posts.md5,
  posts.source,
  posts.rating,
  posts.image_width,
  posts.image_height,
  posts.tag_string,
  posts.locked_tags,
  posts.fav_count,
  posts.file_ext,
  posts.parent_id,
  posts.change_seq,
  posts.approver_id,
  posts.file_size,
  posts.comment_count,
  posts.description,
  posts.duration,
  posts.updated_at,
  posts.is_deleted,
  posts.is_pending,
  posts.is_flagged,
  posts.score,
  posts.up_score,
  posts.down_score,
  posts.is_rating_locked,
  posts.is_status_locked,
  posts.is_note_locked,
  posts.bg_color,
  posts.last_noted_at,
  posts.last_commented_at
FROM posts
ORDER BY posts.id
