<tr>
  <td><span class='title'>Reported User</span></td>
  <td><%= link_to_user(@ticket.content) %> <%= render(UserFeedbackComponent.new(user: @ticket.content, style: :inline)) if CurrentUser.is_staff? %></td>
</tr>
