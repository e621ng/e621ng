<% if @ticket.content %>
    <tr>
      <td><span class='title'>Reported Post</span></td>
      <td><%= PostPresenter.preview(@ticket.content, tags: 'status:any', no_blacklist: true) %></td>
    </tr>
<% end %>
