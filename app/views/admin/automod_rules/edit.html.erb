<% content_for(:page_title) { "Edit Automod Rule: #{@automod_rule.name}" } %>

<div id="c-admin-automod-rules">
  <div id="a-edit">
    <h1>Edit Automod Rule: <%= @automod_rule.name %></h1>

    <%= render "form" %>

    <%= link_to "Back", admin_automod_rules_path %>
  </div>
</div>
