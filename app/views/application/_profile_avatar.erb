<%# locals: (post_id:, user:) %>

<div
  class="profile-avatar placeholder"
  data-id="<%= post_id %>"
  data-name="<%= user.name %>"
  data-initial="<%= user.name[0].capitalize%>"
>
  <span
    class="avatar-image"
    data-initial="<%= user.name[0].capitalize%>"
  ></span>
</div>
