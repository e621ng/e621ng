<div id="c-artists">
  <div id="a-new">
    <h1><%= tm("New {{Artist}}") %></h1>

    <%= error_messages_for :artist %>

    <%= render "form" %>
  </div>
</div>

<%= render "secondary_links" %>

<% content_for(:page_title) do %>
  <%= tm("New {{Artist}}") %>
<% end %>
