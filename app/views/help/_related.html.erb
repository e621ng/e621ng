<% content_for(:secondary_links) do %>
	<% if @related.present? %>
		<li>|</li>
		<% @related.each do |fl| %>
			<li><%= link_to HelpPage.title(fl), action: "show", id: fl.name %></li>
		<% end %>
	<% end %>
<% end %>

